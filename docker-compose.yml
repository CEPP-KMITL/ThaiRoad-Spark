version: '2'

services:
  spark: # Spark Master
    container_name: spark-master
    image: docker.io/bitnami/spark:3.2.1
    environment:
      - SPARK_MODE=master
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
      - SPARK_SSL_ENABLED=no
    ports:
      - '8080:8080'
      - '7077:7077'
    volumes:
    - type: bind
      source: ./conf/log4j.properties
      target: /opt/bitnami/spark/conf/log4j.properties
    - ".:/TRAVIST_Spark:rw" # Name volumn for container to access the local root of this project
    networks:
    - spark
  spark-worker: # Spark Worker
    container_name: spark-worker
    image: docker.io/bitnami/spark:3.2.1
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark:7077
      - SPARK_WORKER_MEMORY=1G
      - SPARK_WORKER_CORES=1
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
      - SPARK_SSL_ENABLED=no
    volumes:
      - type: bind
        source: ./conf/log4j.properties
        target: /opt/bitnami/spark/conf/log4j.properties
    networks:
      - spark
    depends_on:
      - spark

  # spark-livy:
  #   build: ./
  #   command: ["sh", "-c", "/opt/bitnami/livy/bin/livy-server"]
  #   volumes:
  #     - type: bind
  #       source: ./conf/
  #       target: /opt/bitnami/livy/conf/
  #     - type: bind
  #       source: ./target/
  #       target: /target/
  #     - type: bind
  #       source: ./data/
  #       target: /data/
  #   ports:
  #     - '8998:8998'
  #   networks:
  #     - spark
  #   depends_on:
  #     - spark
  #     - spark-worker

networks:
  spark:
    driver: bridge